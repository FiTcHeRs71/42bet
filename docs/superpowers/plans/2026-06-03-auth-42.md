# Auth 42 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Connexion OAuth2 intra.42.fr via NextAuth v5, upsert de la fiche joueur dans `public.users`, session persistante et header avec bouton connexion/déconnexion.

**Architecture:** Logique pure et testable (`mapFt42Profile`, `upsertPlayer`) séparée de la plomberie NextAuth (config + route + composants), comme `src/lib/sync.ts`. L'upsert reçoit ses effets par injection de dépendances (`UpsertDeps`), donc testable avec un fake. La coalition est déférée (`coalition_id` reste `null`).

**Tech Stack:** Next.js 16 (App Router), React 19 (server components + server actions), NextAuth v5 (Auth.js, provider `42-school`), Supabase service_role (`supabaseAdmin`), Vitest.

**Spec :** `docs/superpowers/specs/2026-06-03-auth-42-design.md`

**Prérequis manuel (hors plan) :** une app OAuth doit exister sur
`https://profile.intra.42.fr/oauth/applications` avec redirect URI
`http://localhost:3000/api/auth/callback/42`, et `FT_API_UID` / `FT_API_SECRET`
renseignés dans `.env.local`. Pour `build`/`test`/`typecheck` en local, des
valeurs factices non vides suffisent (les creds ne sont validés qu'au flux OAuth réel).

---

### Task 0: Branche de feature

**Files:** aucun (git).

- [ ] **Step 1: Créer la branche**

```bash
git checkout -b feat/auth-42
```

- [ ] **Step 2: Vérifier le point de départ vert**

Run: `npm test && npm run typecheck && npm run lint`
Expected: tout vert (25 tests, pas d'erreur tsc/eslint).

---

### Task 1: Installer NextAuth v5 + lever les incertitudes

**Files:**
- Modify: `package.json` (dep ajoutée)
- Modify: `.env.local.example` (réconciliation nom de variable secret)
- Modify: `.env.local` (valeurs factices pour build local, NON commité)

- [ ] **Step 1: Installer next-auth v5**

```bash
npm install next-auth@beta
```

- [ ] **Step 2: Vérifier l'existence du provider 42**

Run: `ls node_modules/next-auth/providers/ | grep 42`
Expected: une ligne du type `42-school.js` (et `42-school.d.ts`).
Si absent : abandonner le provider built-in et utiliser une config OAuth manuelle
(authorization `https://api.intra.42.fr/oauth/authorize`, token
`https://api.intra.42.fr/oauth/token`, userinfo `https://api.intra.42.fr/v2/me`).
Noter le résultat avant de continuer.

- [ ] **Step 3: Vérifier le nom de la variable secret attendue par v5**

Run: `grep -rl "AUTH_SECRET" node_modules/next-auth/ node_modules/@auth/ 2>/dev/null | head`
Expected : des occurrences de `AUTH_SECRET` → v5 attend `AUTH_SECRET` (et `AUTH_URL`).

- [ ] **Step 4: Réconcilier `.env.local.example`**

Dans la section `# ---- NextAuth ----`, remplacer les deux lignes
`NEXTAUTH_SECRET=` / `NEXTAUTH_URL=...` par :

```bash
# ---- NextAuth (Auth.js v5) ----
# Générer avec : openssl rand -base64 32
AUTH_SECRET=
# URL de base (optionnelle en dev, inférée). À fixer en prod.
AUTH_URL=http://localhost:3000
```

(Garder `FT_API_UID` / `FT_API_SECRET` / `FT_API_CAMPUS_ID` inchangés.)

- [ ] **Step 5: Renseigner `.env.local` pour le build local**

S'assurer que `.env.local` contient des valeurs non vides pour `AUTH_SECRET`,
`FT_API_UID`, `FT_API_SECRET` (factices acceptées tant que le flux OAuth réel
n'est pas testé). `.env.local` n'est jamais commité.

- [ ] **Step 6: Commit**

```bash
git add package.json package-lock.json .env.local.example
git commit -m "chore(auth): install next-auth v5 and reconcile auth env vars"
```

---

### Task 2: `mapFt42Profile` (fonction pure)

**Files:**
- Create: `src/lib/auth/profile.ts`
- Test: `tests/auth-profile.test.ts`

- [ ] **Step 1: Écrire le test qui échoue**

```ts
// tests/auth-profile.test.ts
import { describe, it, expect } from "vitest";

import { mapFt42Profile } from "@/lib/auth/profile";

describe("mapFt42Profile", () => {
  it("maps a full /v2/me payload", () => {
    expect(
      mapFt42Profile({
        id: 42,
        login: "fducrot",
        image: { link: "https://cdn.intra.42.fr/users/fducrot.jpg" },
      }),
    ).toEqual({
      ftId: 42,
      login: "fducrot",
      avatarUrl: "https://cdn.intra.42.fr/users/fducrot.jpg",
    });
  });

  it("returns null avatar when image is absent", () => {
    expect(mapFt42Profile({ id: 7, login: "norminet" }).avatarUrl).toBeNull();
  });

  it("returns null avatar when image.link is null", () => {
    expect(
      mapFt42Profile({ id: 7, login: "norminet", image: { link: null } })
        .avatarUrl,
    ).toBeNull();
  });
});
```

- [ ] **Step 2: Lancer le test → échec**

Run: `npx vitest run tests/auth-profile.test.ts`
Expected: FAIL — `Cannot find module '@/lib/auth/profile'`.

- [ ] **Step 3: Implémenter le minimum**

```ts
// src/lib/auth/profile.ts
// Pur : normalise la réponse /v2/me de l'API 42 en profil joueur interne.
// Aucun I/O, aucun import server-only — entièrement testable.

/** Sous-ensemble utile de la réponse `GET /v2/me` de l'API 42. */
export interface Ft42Me {
  id: number;
  login: string;
  image?: { link?: string | null } | null;
}

/** Profil joueur normalisé (ce que l'app stocke et expose). */
export interface PlayerProfile {
  ftId: number;
  login: string;
  avatarUrl: string | null;
}

export function mapFt42Profile(raw: Ft42Me): PlayerProfile {
  return {
    ftId: raw.id,
    login: raw.login,
    avatarUrl: raw.image?.link ?? null,
  };
}
```

- [ ] **Step 4: Lancer le test → succès**

Run: `npx vitest run tests/auth-profile.test.ts`
Expected: PASS (3 tests).

- [ ] **Step 5: Commit**

```bash
git add src/lib/auth/profile.ts tests/auth-profile.test.ts
git commit -m "feat(auth): pure mapFt42Profile mapper for /v2/me payloads"
```

---

### Task 3: `upsertPlayer` (I/O par injection de dépendances)

**Files:**
- Create: `src/lib/auth/upsert-player.ts`
- Test: `tests/auth-upsert-player.test.ts`

> Note de nommage : la **fonction** est `upsertPlayer`; la **méthode injectée**
> est `upsertUser` (évite la collision de noms).

- [ ] **Step 1: Écrire le test qui échoue**

```ts
// tests/auth-upsert-player.test.ts
import { describe, it, expect, vi } from "vitest";

import { upsertPlayer } from "@/lib/auth/upsert-player";

const profile = {
  ftId: 42,
  login: "fducrot",
  avatarUrl: "https://cdn.intra.42.fr/users/fducrot.jpg",
};

describe("upsertPlayer", () => {
  it("passes a snake_case row to the injected upsert", async () => {
    const upsertUser = vi.fn().mockResolvedValue({ error: null });
    await upsertPlayer(profile, { upsertUser });
    expect(upsertUser).toHaveBeenCalledWith({
      ft_id: 42,
      login: "fducrot",
      avatar_url: "https://cdn.intra.42.fr/users/fducrot.jpg",
    });
  });

  it("throws (mentioning the login) when the upsert returns an error", async () => {
    const upsertUser = vi.fn().mockResolvedValue({ error: { message: "boom" } });
    await expect(upsertPlayer(profile, { upsertUser })).rejects.toThrow(
      /fducrot/,
    );
  });
});
```

- [ ] **Step 2: Lancer le test → échec**

Run: `npx vitest run tests/auth-upsert-player.test.ts`
Expected: FAIL — `Cannot find module '@/lib/auth/upsert-player'`.

- [ ] **Step 3: Implémenter le minimum**

```ts
// src/lib/auth/upsert-player.ts
// Persistance de la fiche joueur via injection de dépendances : aucune
// référence directe à Supabase ici, donc testable avec un fake. L'appelant
// (config NextAuth) fournit l'implémentation réelle basée sur supabaseAdmin.

import type { PlayerProfile } from "@/lib/auth/profile";

/** Ligne `public.users` que l'on insère/maj (colonnes générées snake_case). */
export interface PlayerRow {
  ft_id: number;
  login: string;
  avatar_url: string | null;
}

export interface UpsertDeps {
  /** Upsert on conflict (ft_id). Renvoie l'erreur Postgrest éventuelle. */
  upsertUser(row: PlayerRow): Promise<{ error: unknown }>;
}

export async function upsertPlayer(
  profile: PlayerProfile,
  deps: UpsertDeps,
): Promise<void> {
  const { error } = await deps.upsertUser({
    ft_id: profile.ftId,
    login: profile.login,
    avatar_url: profile.avatarUrl,
  });
  if (error) {
    throw new Error(`Failed to upsert player ${profile.login}`);
  }
}
```

- [ ] **Step 4: Lancer le test → succès**

Run: `npx vitest run tests/auth-upsert-player.test.ts`
Expected: PASS (2 tests).

- [ ] **Step 5: Commit**

```bash
git add src/lib/auth/upsert-player.ts tests/auth-upsert-player.test.ts
git commit -m "feat(auth): DI-based upsertPlayer that writes public.users"
```

---

### Task 4: Config NextAuth v5 + augmentation de types + route handler

**Files:**
- Create: `src/lib/auth/types.ts`
- Create: `src/lib/auth/config.ts`
- Create: `src/app/api/auth/[...nextauth]/route.ts`

> Pas de test unitaire ici (plomberie d'intégration) : la vérification est
> `typecheck` + `build` à la Task 6. On câble `mapFt42Profile` et `upsertPlayer`
> déjà testés.

- [ ] **Step 1: Augmentation de types (Session / JWT)**

```ts
// src/lib/auth/types.ts
// Étend les types NextAuth pour exposer l'identité 42 sans casts `as`.
import type { DefaultSession } from "next-auth";

declare module "next-auth" {
  interface Session {
    user: {
      ftId: number;
      login: string;
      avatarUrl: string | null;
    } & DefaultSession["user"];
  }
}

declare module "next-auth/jwt" {
  interface JWT {
    ftId?: number;
    login?: string;
    avatarUrl?: string | null;
  }
}

export {};
```

- [ ] **Step 2: Config NextAuth**

```ts
// src/lib/auth/config.ts
// Plomberie NextAuth v5. Server-only : importe supabaseAdmin (service_role).
// Le provider built-in 42-school a son id forcé à "42" pour matcher la redirect
// URI enregistrée (/api/auth/callback/42). Le profil /v2/me est récupéré par
// NextAuth avec le token UTILISATEUR (pas fetch42, qui utilise le token applicatif).
import "server-only";

import NextAuth from "next-auth";
import FortyTwo from "next-auth/providers/42-school";

import { mapFt42Profile, type Ft42Me } from "@/lib/auth/profile";
import { upsertPlayer, type UpsertDeps } from "@/lib/auth/upsert-player";
import { requireEnv } from "@/lib/env";
import { supabaseAdmin } from "@/lib/supabase/server";
import "@/lib/auth/types";

const upsertDeps: UpsertDeps = {
  async upsertUser(row) {
    const { error } = await supabaseAdmin
      .from("users")
      .upsert(row, { onConflict: "ft_id" });
    return { error };
  },
};

export const { handlers, auth, signIn, signOut } = NextAuth({
  providers: [
    {
      ...FortyTwo({
        clientId: requireEnv("FT_API_UID"),
        clientSecret: requireEnv("FT_API_SECRET"),
        profile: (raw: Ft42Me) => {
          const p = mapFt42Profile(raw);
          return { id: String(p.ftId), name: p.login, image: p.avatarUrl };
        },
      }),
      id: "42",
    },
  ],
  callbacks: {
    async signIn({ profile }) {
      if (!profile) return false;
      await upsertPlayer(mapFt42Profile(profile as Ft42Me), upsertDeps);
      return true;
    },
    async jwt({ token, profile }) {
      if (profile) {
        const p = mapFt42Profile(profile as Ft42Me);
        token.ftId = p.ftId;
        token.login = p.login;
        token.avatarUrl = p.avatarUrl;
      }
      return token;
    },
    async session({ session, token }) {
      if (token.ftId != null) {
        session.user.ftId = token.ftId;
        session.user.login = token.login ?? "";
        session.user.avatarUrl = token.avatarUrl ?? null;
      }
      return session;
    },
  },
});
```

> Si le `...FortyTwo({...})` n'accepte pas l'override `id` proprement au
> typecheck, basculer sur la config OAuth manuelle notée en Task 1 Step 2
> (mêmes URLs, même `profile`, `id: "42"`).

- [ ] **Step 3: Route handler**

```ts
// src/app/api/auth/[...nextauth]/route.ts
// Runtime Node (pas edge) : supabaseAdmin + service_role.
import { handlers } from "@/lib/auth/config";

export const { GET, POST } = handlers;
```

- [ ] **Step 4: Typecheck ciblé**

Run: `npm run typecheck`
Expected: pas d'erreur. Si erreur sur l'override `id` du provider ou les
callbacks, appliquer la note du Step 2.

- [ ] **Step 5: Commit**

```bash
git add src/lib/auth/types.ts src/lib/auth/config.ts "src/app/api/auth/[...nextauth]/route.ts"
git commit -m "feat(auth): NextAuth v5 config, 42 provider and auth route handler"
```

---

### Task 5: Header + bouton connexion/déconnexion + nettoyage accueil

**Files:**
- Create: `src/components/auth-button.tsx`
- Create: `src/components/site-header.tsx`
- Modify: `src/app/layout.tsx`
- Modify: `src/app/page.tsx`

- [ ] **Step 1: Bouton d'auth (server component + server actions)**

```tsx
// src/components/auth-button.tsx
// Server component : lit la session et rend soit le bouton de connexion 42,
// soit l'avatar + login + déconnexion. signIn/signOut via server actions.
// <img> brut volontaire : l'hôte de l'avatar 42 varie, on évite de coupler
// next/image à un remotePatterns figé pour cette brique.
import { auth, signIn, signOut } from "@/lib/auth/config";

export async function AuthButton() {
  const session = await auth();

  if (!session?.user) {
    return (
      <form
        action={async () => {
          "use server";
          await signIn("42");
        }}
      >
        <button
          type="submit"
          className="rounded-full bg-black px-4 py-2 text-sm font-medium text-white dark:bg-white dark:text-black"
        >
          Se connecter avec 42
        </button>
      </form>
    );
  }

  const { login, avatarUrl } = session.user;
  return (
    <div className="flex items-center gap-3">
      {avatarUrl && (
        // eslint-disable-next-line @next/next/no-img-element
        <img
          src={avatarUrl}
          alt={login}
          width={32}
          height={32}
          className="rounded-full"
        />
      )}
      <span className="text-sm font-medium">{login}</span>
      <form
        action={async () => {
          "use server";
          await signOut();
        }}
      >
        <button type="submit" className="text-sm text-zinc-500 underline">
          Déconnexion
        </button>
      </form>
    </div>
  );
}
```

- [ ] **Step 2: Header du site**

```tsx
// src/components/site-header.tsx
import { AuthButton } from "@/components/auth-button";

export function SiteHeader() {
  return (
    <header className="flex items-center justify-between border-b border-black/10 px-6 py-4 dark:border-white/10">
      <a href="/" className="text-lg font-bold tracking-tight">
        42Bet
      </a>
      <AuthButton />
    </header>
  );
}
```

- [ ] **Step 3: Monter le header + corriger les métadonnées**

Dans `src/app/layout.tsx` :
- Ajouter l'import : `import { SiteHeader } from "@/components/site-header";`
- Remplacer le bloc `metadata` par :

```ts
export const metadata: Metadata = {
  title: "42Bet — Pronostics Coupe du Monde",
  description: "Pronostics foot sans argent réel pour l'École 42 Lausanne.",
};
```

- Remplacer `<body className="min-h-full flex flex-col">{children}</body>` par :

```tsx
<body className="min-h-full flex flex-col">
  <SiteHeader />
  {children}
</body>
```

- [ ] **Step 4: Accueil sobre**

Remplacer **tout** le contenu de `src/app/page.tsx` par :

```tsx
// src/app/page.tsx
import { auth } from "@/lib/auth/config";

export default async function Home() {
  const session = await auth();

  return (
    <main className="flex flex-1 flex-col items-center justify-center gap-4 p-8 text-center">
      <h1 className="text-4xl font-bold tracking-tight">42Bet</h1>
      <p className="text-zinc-500">
        Pronostics Coupe du Monde — École 42 Lausanne
      </p>
      {session?.user ? (
        <p>
          Connecté en tant que <strong>{session.user.login}</strong>.
        </p>
      ) : (
        <p>Connecte-toi avec ton compte 42 pour parier.</p>
      )}
    </main>
  );
}
```

- [ ] **Step 5: Vérifs lint + typecheck**

Run: `npm run lint && npm run typecheck`
Expected: pas d'erreur (le `<img>` est couvert par le `eslint-disable-next-line`).

- [ ] **Step 6: Commit**

```bash
git add src/components/auth-button.tsx src/components/site-header.tsx src/app/layout.tsx src/app/page.tsx
git commit -m "feat(auth): site header with 42 sign-in/out and sober home page"
```

---

### Task 6: Annotation skill, vérification complète, merge

**Files:**
- Modify: `skills/42api-fetch/SKILL.md`

- [ ] **Step 1: Documenter l'exception `/v2/me` au sign-in**

Dans `skills/42api-fetch/SKILL.md`, après la règle 1 (Rate limit) ou dans la
section « Endpoints utilisés », ajouter une note :

```markdown
> **Exception assumée** : pendant le sign-in OAuth (NextAuth), `/v2/me` est
> appelé par NextAuth avec le **token utilisateur** (issu de l'échange OAuth),
> pas via `fetch42()` (qui utilise le token applicatif `client_credentials`).
> C'est le seul appel API 42 hors wrapper. Voir `src/lib/auth/config.ts`.
```

- [ ] **Step 2: Vérification complète**

Run: `npm test && npm run typecheck && npm run lint && npm run build`
Expected:
- tests : 30 verts (25 existants + 3 profile + 2 upsert).
- typecheck/lint : pas d'erreur.
- build : succès, route `/api/auth/[...nextauth]` enregistrée (ƒ Dynamic).

Si `build` échoue sur `requireEnv` (FT_API_UID/SECRET/AUTH_SECRET vide),
renseigner des valeurs factices non vides dans `.env.local` puis relancer.

- [ ] **Step 3: Commit de la skill**

```bash
git add skills/42api-fetch/SKILL.md
git commit -m "docs(auth): note the sign-in /v2/me exception in 42api-fetch skill"
```

- [ ] **Step 4: Self-review du diff**

Run: `git diff main...feat/auth-42 --stat`
Vérifier : aucun secret commité, `.env.local` absent du diff, fichiers attendus seulement.

- [ ] **Step 5: Merge dans main (workflow pré-déploiement)**

```bash
git checkout main
git merge --no-ff feat/auth-42 -m "merge: 42 OAuth auth brick (NextAuth v5 → public.users)"
git branch -d feat/auth-42
```

- [ ] **Step 6: Vérification post-merge**

Run: `npm test && npm run typecheck && npm run lint`
Expected: tout vert sur `main`.

---

## Definition of Done (rappel spec §10)

- [ ] `next-auth@beta` installé ; `.env.local.example` réconcilié (`AUTH_SECRET`/`AUTH_URL`).
- [ ] `mapFt42Profile` + `upsertPlayer` testés (rouge → vert).
- [ ] Config NextAuth v5 + route `[...nextauth]` ; provider 42 (id `"42"`).
- [ ] Header connexion/déconnexion ; `page.tsx` + `layout.tsx` nettoyés.
- [ ] `npm test` + `typecheck` + `lint` verts ; `build` OK.
- [ ] Skill `42api-fetch` annotée.
- [ ] Merge `--no-ff` dans `main`.

## Vérification manuelle du flux (hors CI, nécessite vraies creds 42)

1. `.env.local` avec vraies `FT_API_UID`/`FT_API_SECRET` + app OAuth enregistrée.
2. `npm run dev`, ouvrir `http://localhost:3000`.
3. « Se connecter avec 42 » → OAuth intra → retour connecté.
4. Vérifier dans Supabase que `public.users` contient une ligne (`ft_id`, `login`,
   `avatar_url`, `coalition_id` null).
5. Header affiche avatar + login ; « Déconnexion » revient à l'état anonyme.
